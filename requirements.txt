# ===========================================
# CORE
# ===========================================
Flask>=3.1.2,<4
python-dotenv>=1.1.1,<2
gunicorn>=23.0.0,<24

# ===========================================
# SECURITY
# ===========================================
flask-cors>=6.0.1,<7
Flask-Limiter>=4.0.0,<5
# PyJWT>=2.9.0,<3

# ===========================================
# DATABASE
# ===========================================
# Modo A — DB_ENABLED=true, ORM_ENABLED=false (PyMySQL directo)
#   Descomentar el driver de tu motor:
# PyMySQL>=1.1.2,<2
# psycopg2-binary>=2.9.9,<3

# Modo B — DB_ENABLED=true, ORM_ENABLED=true (SQLAlchemy)
#   Descomentar Flask-SQLAlchemy + driver:
# Flask-SQLAlchemy>=3.1.1,<4
# PyMySQL>=1.1.2,<2          # driver mysql
# psycopg2-binary>=2.9.9,<3  # driver postgresql

# ===========================================
# VALIDATION
# ===========================================
# Modo A — marshmallow puro
# marshmallow>=3.23.0,<4

# Modo B — flask-marshmallow + bindings ORM
# Flask-Marshmallow>=1.3.0,<2
# marshmallow-sqlalchemy>=1.4.2,<2

# ===========================================
# EMAIL
# ===========================================
# Flask-Mailman>=1.1.1,<2

# ===========================================
# CONCURRENCY / WEBSOCKETS
# ===========================================
# gevent>=25.9.1,<26                 # workers async para alta concurrencia
# flask-socketio>=5.5.0,<6           # websockets: requiere gevent descomentado
# simple-websocket>=1.0,<2           # transporte websocket recomendado por flask-socketio

# ===========================================
# BROWSER AUTOMATION
# ===========================================
# playwright>=1.55.0,<2